export enum Errors {
  NOT_FOUND = 'An error occured while loading the data :(',
}

export enum Common {
  EMPTY_LIST = 'List of animal cards is empty!',
  SHOW_LIKED_CARDS = 'Show liked cards',
  SHOW_ALL_CARDS = 'Show all cards',
}
